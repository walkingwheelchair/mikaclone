apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-python-ide-to-minio
  namespace: data-storage
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: minio
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: python-ide
    ports:
    - protocol: TCP
      port: 9000
